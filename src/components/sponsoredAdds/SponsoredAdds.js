import React from "react";
import sponsoredboxStyles from "./SponsoredAdds.module.css";
const SponsoredAdds = () => {
  return <div className={sponsoredboxStyles.sponsoredbox}>SponsoredAdds</div>;
};

export default SponsoredAdds;
